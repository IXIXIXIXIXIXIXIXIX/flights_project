export interface AvailableLanguagesResponse {
    languages: {
        code: string;
        name: string;
        nativeName: string;
    }[];
}
export declare const availableLanguages: (signal?: AbortSignal | undefined) => Promise<AvailableLanguagesResponse>;
