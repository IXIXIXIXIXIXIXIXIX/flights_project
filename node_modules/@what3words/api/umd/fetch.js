var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "./utils", "axios", "./version", "os"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.fetchGet = void 0;
    var utils_1 = require("./utils");
    var axios_1 = require("axios");
    var version_1 = require("./version");
    var os = require("os");
    var platform = utils_1.getPlatform(os.platform());
    exports.fetchGet = function (url, data, signal) {
        if (data === void 0) { data = {}; }
        if (signal === void 0) { signal = null; }
        return new Promise(function (resolve, error) {
            var headers = {
                'X-W3W-Wrapper': "what3words-Node/" + version_1.version + " (Node " + process.version + "; " + platform + " " + os.release() + ")"
            };
            if (typeof utils_1.GLOBAL_OPTIONS.key === "string" &&
                utils_1.GLOBAL_OPTIONS.key.length > 0) {
                data["key"] = utils_1.GLOBAL_OPTIONS.key;
            }
            if (utils_1.GLOBAL_OPTIONS.headers) {
                headers = __assign(__assign({}, headers), utils_1.GLOBAL_OPTIONS.headers);
            }
            axios_1.default.request({
                url: utils_1.GLOBAL_OPTIONS.baseUrl + "/" + url + "?" + utils_1.searchParams(data),
                headers: headers
            })
                .then(function (response) {
                if (response && response.status) {
                    if (response.status >= 400) {
                        // @ts-ignore
                        error(response.data.error);
                    }
                }
                resolve(response.data);
            }).catch(function (data) {
                if (data.response) {
                    error(data.response.data);
                }
                else {
                    error(data.message);
                }
            });
        });
    };
});
