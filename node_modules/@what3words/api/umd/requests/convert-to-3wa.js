(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "../utils", "../fetch"], factory);
    }
})(function (require, exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.convertTo3waGeoJson = exports.convertTo3wa = void 0;
    var utils_1 = require("../utils");
    var fetch_1 = require("../fetch");
    var convertTo3waBase = function (coordinates, language, format, signal) {
        var requestOptions = {
            coordinates: utils_1.coordinatesToString(coordinates)
        };
        if (language !== undefined) {
            requestOptions["language"] = language;
        }
        if (format !== undefined) {
            requestOptions["format"] = format;
        }
        return fetch_1.fetchGet("convert-to-3wa", requestOptions, signal);
    };
    exports.convertTo3wa = function (coordinates, language, signal) {
        return convertTo3waBase(coordinates, language, "json", signal);
    };
    exports.convertTo3waGeoJson = function (coordinates, language, signal) {
        return convertTo3waBase(coordinates, language, "geojson", signal);
    };
});
